{
  "name": "clocky",
  "version": "1.0.0",
  "description": "unusual visualization of your Google calendar schedule",
  "main": "app.js",
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:prod":"copy .\\config\\config-prod.js .\\src\\config.js && firebase use prod && firebase deploy --only hosting",
    "dev":"copy .\\config\\config-dev.js .\\src\\config.js && cd src && live-server --host=localhost"
  },
  "author": "",
  "license": "ISC"
}
